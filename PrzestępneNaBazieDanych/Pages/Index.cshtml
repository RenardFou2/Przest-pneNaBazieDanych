@page
@model PrzestępneNaBazieDanych.Pages.IndexModel

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-page="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Przestepne[0].Year)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Przestepne[0].Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Przestepne[0].Date)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Przestepne[0].Result)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Przestepne[0].UserLogin)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Przestepne[0].IDofUser)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model.Przestepne) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Year)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Date)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Result)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.UserLogin)
            </td>
            <td>
                    @Html.DisplayFor(modelItem => item.IDofUser)
            </td>
            <td>
                <a asp-page="./Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-page="./Details" asp-route-id="@item.Id">Details</a> |
                <a asp-page="./Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
